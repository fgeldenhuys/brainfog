# CLAUDE.md

Read `AGENTS.md` first. It is the canonical project instruction file.

This repository is brainfog's own codebase, not a connected memory store. When working here, do not call brainfog's MCP tools against this repo's own ASDLC artifacts — `specs/`, `docs/adrs/`, and `tasks/` here are this project's source of truth, per `ARCHITECTURE.md`.
